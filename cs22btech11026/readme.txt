COMPILATION INSTRUCTIONS:
1)In a terminal compile the code by running "gcc -o exec assn.c -lm"
2)An executable file is created by the name"exec"
3)Run the executable file by running "./exec"

NOTE : All parts(1,2,3,4) are supported 

INPUT:
1)Make sure both the conifg,input files are named properly{cache.config , cache.access}
2)All input conditions and formats are as given in problem statement.

OUTPUT:
1)Output are as given in problem statement additionally i printed number of hits and misses
